#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "=== [CI 검증 실행] 빌드 및 회귀 방지 체크 ==="

# 1. 실제 프로젝트 빌드 구동 (CSS 및 소스코드 문법 에러 엄격 통제)
npm run build || { echo "❌ 빌드 실패: 문법 에러가 존재하므로 커밋이 취소되었습니다."; exit 1; }

# 2. 기존 테스트 스크립트 실행
npm test || { echo "❌ 회귀 테스트 실패: 커밋이 취소되었습니다."; exit 1; }