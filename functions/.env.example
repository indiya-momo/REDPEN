# 클라이언트 VITE_BETA_QUOTA_ADMIN_* 와 동일한 값 (쉼표 구분)
# 배포: Firebase Console → Functions → 환경 변수 또는 `firebase functions:secrets` /
# functions/.env (로컬 에뮬·배포 시 firebase-tools가 주입)
BETA_QUOTA_ADMIN_UIDS=
BETA_QUOTA_ADMIN_EMAILS=
